export type {
  ComponentCategory,
  ComponentData,
  ComponentRecord,
  ComponentsResponse,
} from './catalog-core'

export interface ComponentCategoryInfo {
  id: string
  name: string
  icon: string
  count: number
}
